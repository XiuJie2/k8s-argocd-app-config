apiVersion: v1
kind: Service
metadata:
  name: ntub-service
  namespace: ntub
spec:
  type: NodePort
  selector:
    app: ntub
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080