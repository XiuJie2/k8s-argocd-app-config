apiVersion: v1
kind: Service
metadata:
  name: ntub-service
  namespace: ntub
spec:
  type: NodePort
  selector:
    app: ntub
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
